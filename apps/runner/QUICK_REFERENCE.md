# 🚀 DeepGiBox Quick Reference

## เริ่มใช้งานเลย! (30 วินาที)

### 1️⃣ เลือกกล้อง
```bash
# Pentax
./target/release/runner configs/runner_pentax.toml

# Olympus  
./target/release/runner configs/runner_olympus.toml

# Fuji
./target/release/runner configs/runner_fuji.toml
```

### 2️⃣ หยุดโปรแกรม
```
กด Ctrl+C
```

### 3️⃣ ดูผลลัพธ์
```
FPS: 33.76        <- เฟรมต่อวินาที (ยิ่งมากยิ่งดี)
Latency: 29.62ms  <- ความหน่วง (ยิ่งน้อยยิ่งดี)
Detections: 1     <- เจอกี่ชิ้น
```

---

## สี Bounding Box

| สี | ความหมาย | Confidence |
|----|----------|------------|
| 🟢 เขียว | มั่นใจมาก | > 0.3 |
| 🟡 เหลือง | มั่นใจปานกลาง | 0.2-0.3 |
| 🔴 แดง | มั่นใจน้อย | < 0.2 |

---

## ปรับแต่งเร็ว

### เปลี่ยน Sensitivity
```bash
nano configs/runner_pentax.toml
```

```toml
[inference]
confidence_threshold = 0.15  # เลข↑ = เข้มงวด, เลข↓ = ไว
```

| Threshold | ผลลัพธ์ |
|-----------|---------|
| 0.10 | ไวมาก (ตรวจจับเยอะ แต่อาจผิดพลาด) |
| 0.15 | ปานกลาง (แนะนำ) ✓ |
| 0.20 | เข้มงวด (ตรวจจับน้อย แต่แม่นยำ) |
| 0.25 | เข้มงวดมาก |

### เปลี่ยนบริเวณตรวจจับ (Crop Region)
```toml
[preprocessing]
crop_region = [x, y, width, height]
```

| กล้อง | Crop Region (แนะนำ) |
|-------|---------------------|
| Pentax | `[780, 182, 752, 752]` |
| Olympus | `[830, 330, 655, 490]` |
| Fuji | `[1032, 326, 848, 848]` |

---

## แก้ปัญหาด่วน

### โปรแกรมไม่เริ่ม
```bash
# เช็ค DeckLink
ls /dev/blackmagic/

# เช็ค GPU
nvidia-smi
```

### FPS ต่ำ (< 20)
```toml
[inference]
confidence_threshold = 0.20  # เพิ่มค่า
use_fp16 = true              # เปิด FP16
```

### ตรวจจับไม่เจอ
```toml
[inference]
confidence_threshold = 0.10  # ลดค่า
```

### ตรวจจับผิดพลาดเยอะ
```toml
[inference]
confidence_threshold = 0.25  # เพิ่มค่า

[postprocessing]
nms_iou_threshold = 0.3      # ลดค่า (เดิม 0.5)
```

---

## ทดสอบระบบ

```bash
# ทดสอบเร็ว (5-10 วินาที)
./test_all_features.sh

# ทดสอบละเอียด
./test_comprehensive.sh
```

---

## Performance ที่ดี

| Metric | ค่าดี | ค่าไม่ดี |
|--------|-------|----------|
| FPS | > 30 ✓ | < 20 ✗ |
| Latency | < 40ms ✓ | > 60ms ✗ |
| Preprocessing | < 2ms ✓ | > 5ms ✗ |
| Inference | < 10ms ✓ | > 20ms ✗ |

---

## คำสั่งที่ใช้บ่อย

```bash
# Build ใหม่
cargo build --release --bin runner

# ดู GPU
watch -n 1 nvidia-smi

# Backup config
cp configs/runner_pentax.toml configs/backup.toml

# Log statistics
./target/release/runner configs/runner_pentax.toml 2>&1 | tee log.txt
```

---

## เอกสารเพิ่มเติม

| ไฟล์ | เนื้อหา |
|------|---------|
| `HOW_TO_USE.md` | คู่มือใช้งานฉบับเต็ม (ไทย) |
| `RUNNER_QUICK_START.md` | Quick Start (ไทย) |
| `FEATURE_TEST_GUIDE.md` | Feature Testing (อังกฤษ) |
| `apps/runner/README.md` | Technical Docs (อังกฤษ) |

---

## Cheat Sheet

```bash
# เริ่ม
./target/release/runner configs/runner_pentax.toml

# หยุด
Ctrl+C

# แก้ config
nano configs/runner_pentax.toml

# ทดสอบ
./test_all_features.sh

# Build
cargo build --release --bin runner

# ดู GPU
nvidia-smi
```

---

**ใช้งานได้เลย! 🎉**

_Last updated: November 6, 2025_
